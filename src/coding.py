class Coding:
    def __init__(self, system: str, version: str, code: str, display: str, user_selected: bool) -> None:
        self.system = system
        self.version = version
        self.code = code
        self.display = display
        self.user_selected = user_selected
